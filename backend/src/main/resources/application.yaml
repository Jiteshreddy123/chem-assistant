spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/chem_assistant
    username: postgres
    password: Jitesh@1cj
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
